# Лабораторні роботи з C++ та веб-інтерфейсом

Цей проект містить 11 лабораторних робіт з веб-інтерфейсом та C++ бекендом.

## 🚀 Швидкий старт

### Варіант 1: Автоматичний перезапуск (рекомендовано)

Запустіть скрипт автоматичного перезапуску - він буде автоматично компілювати та перезапускати сервер при змінах у файлах:

```bash
auto_restart_main.bat    # Для головного сервера
# або
auto_restart.bat         # З вибором типу сервера
```

### Варіант 2: Ручний запуск

1. **Компілюйте головний сервер:**
   ```bash
   compile_main.bat
   ```

2. **Запустіть головний сервер:**
   ```bash
   run_main_server.bat
   ```

3. **Відкрийте браузер:**
   ```
   http://localhost:8080
   ```

4. **Виберіть лабораторну роботу** з головної сторінки!

## Структура проекту

```
LAB__C++/
├── main_server.cpp      # Головний сервер з роутингом
├── main_server.exe      # Скомпільований головний сервер
├── index.html           # Головна сторінка з вибором лабораторних
├── static/              # Статичні файли (CSS, JS)
│   ├── style.css
│   └── script.js
├── lab1/                # Лабораторна робота 1
├── lab2/                # Лабораторна робота 2
├── ...
├── lab11/               # Лабораторна робота 11
└── common/              # Спільні файли (бібліотеки, утиліти)
```

## Вимоги

- CMake 3.10 або вище
- C++17 компілятор (GCC, Clang, або MSVC)
- На Windows: Winsock2 (включено в систему)
- На Linux/Mac: стандартні системні бібліотеки

## Збірка проекту

```bash
mkdir build
cd build
cmake ..
cmake --build .
```

## Запуск лабораторної роботи

### Варіант 1: Через головний сервер (рекомендовано)

Головний сервер дозволяє вибирати лабораторну роботу через веб-інтерфейс:

```bash
compile_main.bat    # Компіляція
run_main_server.bat # Запуск
```

Потім відкрийте браузер: `http://localhost:8080`

### Варіант 2: Окремий запуск кожної лабораторної

Кожну лабораторну роботу можна запустити окремо:

```bash
cd lab2
compile.bat      # Компіляція
run_server.bat   # Запуск
```

Потім відкрийте браузер: `http://localhost:8080`

## Структура лабораторної роботи

Кожна лабораторна робота містить:
- `main.cpp` - основний файл з C++ бекендом
- `CMakeLists.txt` - файл збірки для цієї лабораторної
- `web/` - папка з веб-інтерфейсом (HTML, CSS, JS)
  - `index.html` - головна сторінка
  - `style.css` - стилі
  - `script.js` - JavaScript код

## Спільні компоненти

- `common/server_base.h` - базовий клас HTTP сервера, який можна використовувати в усіх лабораторних роботах
- `main_server.cpp` - головний сервер з роутингом для всіх лабораторних робіт

## Особливості

- **Головний сервер** - один сервер для всіх лабораторних робіт з красивим інтерфейсом вибору
- **Консольний дизайн** - темна тема з моноширинним шрифтом
- **Роутинг** - автоматичне перенаправлення на відповідну лабораторну роботу
- **Сервер запускається на порту 8080**
- Всі лабораторні роботи використовують однакову базову структуру

## Навігація

- `http://localhost:8080` - головна сторінка з вибором лабораторних робіт
- `http://localhost:8080/lab1/index.html` - лабораторна робота 1
- `http://localhost:8080/lab2/index.html` - лабораторна робота 2
- і так далі...

